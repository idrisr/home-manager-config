{
  programs.opencode = {
    enable = true;
    settings = {
      # model = "kimi-for-coding/k2p5";
      autoshare = false;
      autoupdate = true;
      # web = {
      # enable = true;
      # extraArgs = [
      # "--hostname"
      # "0.0.0.0"
      # "--port"
      # "4096"
      # "--mdns"
      # "--cors"
      # "https://example.com"
      # "--cors"
      # "http://localhost:3000"
      # "--print-logs"
      # "--log-level"
      # "DEBUG"
      # ];
      # };
    };
  };
}
